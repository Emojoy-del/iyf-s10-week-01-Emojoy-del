# DevTools Exploration Answers

## Website 1: https://example.com

- HTML tags used on the page:
  - `<!DOCTYPE html>`
  - `<html>`
  - `<head>`
  - `<meta>`
  - `<title>`
  - `<body>`
  - `<div>`
  - `<h1>`
  - `<p>`
  - `<a>`
- Page title:
  - `Example Domain`
- How many headings are there:
  - 1 heading (`<h1>`)

## Website 2: https://developer.mozilla.org

- Navigation menu wrapper tag:
  - The main navigation is wrapped in a `<nav>` element.
- Search bar structure:
  - The search bar is implemented as a form-like block, typically using an `<input type="search">` or `<input type="text">` plus a submit button inside a search container.
- Hover effect on links:
  - On hover, links usually change appearance via CSS, such as a color change and/or underline, indicating the link is interactive.

## Website 3: Any website of my choice

- Website chosen: https://example.com
- Five different HTML elements identified:
  1. `<html>`
  2. `<head>`
  3. `<body>`
  4. `<h1>`
  5. `<a>`
- Form element and inputs found:
  - This website does not contain a form element, so no form inputs are present.

## Screenshot

- Screenshot of the Elements panel:
  - Screenshot capture not available here. Please take a screenshot in the browser DevTools Elements panel and attach it separately if required.
